hl.monitor({
	output = "",
	mode = "preferred",
	position = "auto",
	scale = "auto",
})

hl.config(require("cfg.base"))

require("cfg.env")
require("cfg.animations")
require("cfg.binds")
require("cfg.window-rules")
require("cfg.workspaces")
